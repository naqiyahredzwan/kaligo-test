'use strict';

describe('kaligoApp.assignment module', function() {

  beforeEach(module('kaligoApp.assignment'));

  describe('kaligoApp assignment controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var assignmentCtrl = $controller('assignmentCtrl');
      expect(assignmentCtrl).toBeDefined();
    }));

  });
});